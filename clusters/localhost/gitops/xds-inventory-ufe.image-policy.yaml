apiVersion: image.toolkit.fluxcd.io/v1
kind: ImagePolicy
metadata:
  name: inventory-ufe
  namespace: wac-hospital
spec:
  imageRepositoryRef:
    name: inventory-ufe
  filterTags:
    pattern: "main.*"
  policy:
    alphabetical:
      order: asc
